1、权限里面的 admin逻辑处理已经加了，tenent相关的权限还没加
2、跨agent访问
3、初始化分类
4、去掉无用的初始化代码
5、去掉无用的拦截器若干，包括对tokenIner 和 agentInter


6、AutoWebMvcConfiguration里面的exclued可能配置的不对

7、tenant-role-permission 在 spring securiy 相关逻辑
8、tennat-user-role 在 spring securiy相关逻辑

10、RedisKeys 处理

11、token 中区分普通用户类型 用户id

12、DefaultUserContext 和 TokenUserDetail的合并可能性
13、createTenantToken 有优化的空间
loadTenantUserByUsername和checkTenant有冲突

14、queryByid 改为 findById统一持久层命名规则

