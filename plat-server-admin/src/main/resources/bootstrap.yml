spring:
  application:
    name: plat-server-admin
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848

    sentinel:
      transport:
        port: 8719
        dashboard:  127.0.0.1:8080
      datasource:
        ds:
          nacos:
            server-addr: 127.0.0.1:8848
            dataId: ${spring.application.name}-sentinel
            groupId: DEFAULT_GROUP
            rule-type: flow
feign:
  sentinel:
    enabled: true