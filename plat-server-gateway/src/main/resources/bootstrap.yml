spring:
  application:
    name: plat-server-gateway
  config:
    import:
      - optional:nacos:plat-common.yaml
      - optional:nacos:plat-server-gateway-local.yaml
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: local
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: local
